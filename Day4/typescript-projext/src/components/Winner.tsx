import React, { ReactNode } from 'react'

type WinnerProps = {
    children : ReactNode
}

function Winner(props: WinnerProps) {
  return (
    <div>
      And the winer is Mo Salah
    </div>
  )
}

export default Winner
